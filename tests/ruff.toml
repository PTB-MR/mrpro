extend = "../pyproject.toml"

lint.extend-select = ["PT"] # flake8-pytest-style

lint.extend-ignore = [
    "ANN",   # flake8-annotations
    "D",     # pydocstyle
    "N",     # pep8-naming
    "S101",  # use of assert detected
    "PT011", # pytest-raises-too-broad
]
